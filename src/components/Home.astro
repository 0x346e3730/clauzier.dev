---
import { Icon } from 'astro-icon/components';
import { calculateExperience } from '../utils/experience';
import TerminalPrompt from './TerminalPrompt.astro';

const birthDate = new Date('1998-05-22'); // Your birthdate
const today = new Date();
const age = today.getFullYear() - birthDate.getFullYear() - 
    (today.getMonth() < birthDate.getMonth() || 
    (today.getMonth() === birthDate.getMonth() && today.getDate() < birthDate.getDate()) ? 1 : 0);

const experience = calculateExperience();
---

<TerminalPrompt command="whoami" />
<div class="text-base leading-relaxed whitespace-pre-wrap mb-6">Antonin CLAUZIER, <span class="metadata">{age} years old</span> passionate Lead Dev based in <span class="metadata">Saint-Denis, La Réunion</span>.</div>

<TerminalPrompt command="cat about.md" />
<div class="text-base leading-relaxed whitespace-pre-wrap"><span class="metadata">{experience.years}+ years</span> of experience in the tech industry, especially e-commerce and healthcare.
Also co-founded and been the president of the biggest e-sports fan club in the world.
Now offering some of my free time, knowledge and skillsets to an association that saves animals.</div>
